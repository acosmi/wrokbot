# Wrok Bot mobile client

Shared uni-app source for the mobile client. Native authentication, device pairing, and platform integrations are incomplete. The default client reports unavailable features explicitly and does not send authenticated requests until the server credential contract is available.

Use the project configuration and platform tooling to build the desired target. Generated `unpackage/` output and local signing credentials are excluded from version control.
