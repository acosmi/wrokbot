# Database schema fixtures

Normalized schema snapshots consumed by database migration and compatibility tests. Snapshot filenames identify the schema version. Update a snapshot only alongside the corresponding SQL migration and verify the database tests against PostgreSQL.
