# AG-UI schema fixtures

Official AG-UI 0.0.57 schema source files. The adjacent provenance file records the upstream Git blobs, source locations, and SHA-256 hashes. Preserve upstream license notices when updating these fixtures.
